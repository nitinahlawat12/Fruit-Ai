import React from 'react';
import Chatbox from './Chatbox'; // Ensure this import path is correct

const ChatboxPage = () => {
    return (
        <div className="chatbox-page">
            <h1>Chat with Us</h1>
            <Chatbox />
        </div>
    );
};

export default ChatboxPage;
